[pytest]
markers =
    testrail: mark a testrail cases
    lp: mark для регресса Live Plus

filterwarnings =
    ignore:.*U.*mode is deprecated:DeprecationWarning

log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli=true
log_level=INFO